# X.3 — Agent Learning Track

The agent's actual source code. Previous contents (a token-frequency state-vector agent used to produce [Paper #1](../X4-papers/README.md)) were intentionally reset to rebuild cleanly alongside the new curriculum.

Fires when a curriculum module teaches a transferable learning mechanism (state, update rule, feedback, memory). Promoted ideas come from [curriculum/seedlings/X3-agent-seeds.md](../../curriculum/seedlings/X3-agent-seeds.md).

Nothing implemented yet — next up per the seedling log: `normalize()` (L1/L2) and `cosine_similarity()` from scratch (Tier0-M1), once Tier 0 has enough modules to justify the first rebuild.
