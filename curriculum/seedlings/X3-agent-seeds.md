# X.3 Seedling Log — Agent Code Ideas

One line per module: "if this idea touched the agent's learning code, it would look like ___."
Reviewed at the end of every tier (X.6); promoted seeds move into `tracks/X3-agent/`.

| Module | Seed |
|---|---|
| Tier0-M0 Touch Typing | Audit where the agent spends compute on low-value bottleneck ops (I/O/parsing) vs. actual learning — its version of not touch-typing. |
| Tier0-M1 Linear Algebra Foundations | Implement `normalize()` (L1 & L2) and `cosine_similarity()` from scratch as the agent's foundational state-comparison primitives. |
